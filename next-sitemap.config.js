/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: "https://eduardobrito.dev", // Troque pelo seu domínio
    generateRobotsTxt: true, // Gera o robots.txt automaticamente
    sitemapSize: 5000, // Número máximo de URLs por sitemap
  };
  